import TodoListItem from "./TodoListItem"

const TodoList = () => {
    return (
        <div>
            <h2>I am a child</h2>
            <TodoListItem/>
        </div>
    )
}

export default TodoList
